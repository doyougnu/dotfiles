xkb_symbols "dyg-dvorak"
{
  // include "us(dvp)"            // dvorak programmer
  include "us(dvorak)"            // includes the base US keys

  // commenting all these out because I'm implementing these in kmonad now
  // but I want to keep control over the layout itself in xserver, so we'll
  // keep this file
  // key.type = "ONE_LEVEL";
  // key <RALT>  { [ Return    ] };
  // key <CAPS>  { [ Control_R ] };
 // key <TAB>   { [ Escape    ] };
  // key <RTSH>  { [ Caps_Lock ] };
  // key <RCTL>  { [ Tab       ] };
  // key <RTRN>  { [ BackSpace ] };
  // key <BKSP>  { [ Tab       ] };
};
